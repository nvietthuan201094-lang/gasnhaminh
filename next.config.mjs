/** @type {import('next').NextConfig} */
const nextConfig = {
  // Chuẩn hóa URL không có dấu gạch chéo cuối để tránh trùng lặp nội dung và chuỗi chuyển hướng
  trailingSlash: false,
};

export default nextConfig;
